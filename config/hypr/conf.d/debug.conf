# =============================================================================
# TEMPORARY DIAGNOSTIC FILE — investigating CBackend::create() crash, 2026-09-04.
# Remove once root cause is confirmed and fixed.
# First attempt used debug:disable_logs, which is the wrong option name for
# this Hyprland build - the runtime message itself says
# "debug.enable_stdout_logs = 0", not disable_logs. Corrected below.
# =============================================================================
debug {
    disable_logs = false
    enable_stdout_logs = true
}
